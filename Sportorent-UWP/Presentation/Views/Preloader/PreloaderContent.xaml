<Page
    x:Class="DronZone_UWP.Presentation.Views.Preloader.PreloaderContent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Sportorent_UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="CornflowerBlue" Opacity="0.9" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <StackPanel Orientation="Horizontal"
                    Padding="12"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center">
            <Grid Margin="0,0,8,0">
                <ProgressRing IsActive="True" Foreground="Blue" />
            </Grid>
            <TextBlock Text="It's ok, we are loading it :)" 
                       Foreground="Black"
                       FontSize="24"
                       FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</Page>
